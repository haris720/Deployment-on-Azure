{ "navigationFallback": { "rewrite": "/index.html", "exclude": [ "/assets/*", "/*.{png,jpg,jpeg,gif,svg,css,js}" ]
  },
  "globalHeaders": { "Cache-Control": "no-cache"
  },
  "responseOverrides": { "404": { "rewrite": "/index.html"
    }
  }
}
